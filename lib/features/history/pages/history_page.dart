import 'package:flutter/material.dart';

class HistoryPage extends StatelessWidget {
  const HistoryPage({super.key});

  /// route path
  static const String path = '/history-page';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
